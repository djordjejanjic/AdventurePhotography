// MDB Lightbox Init
const main = document.querySelector('.main');
const avantura = document.querySelector('.avantura');


function init() {
    setTimeout(() => {
        setTimeout(() => (main.style.opacity = 1), 50);
        logo.style.opacity = 1;
    }, 200)
    setTimeout(() => {
        setTimeout(() => (avantura.style.color = "rgb(249 50 50)"), 50);
    }, 800)

}

init();